version: '3.7'

services:
  family-coffee-db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_USER: mysql
      MYSQL_PASSWORD: root
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./data:/var/lib/mysql
    ports:
      - "33065:3306"
